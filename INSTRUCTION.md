Clone repository 
git clone (write here url repository)

run command for starting app  
docker-compose up -d

Go to web page in browser
http://localhost:8080

run command for stoped app  
docker-compose stop

for stop and remove container use command
docker-compose down -v

for chech running container use command
docker ps

for view logs use this command
docker-compose logs
